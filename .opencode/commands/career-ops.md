---
description: Career-Ops router from repo parent root
---

Repo parent-root shim.

Delegate to the nested Career-Ops command surface in `career-ops/.opencode/commands/`.

Behavior parity:
- empty args -> discovery menu
- JD/URL -> auto-pipeline
- mode args -> matching nested Career-Ops command/mode

If you launched OpenCode from `CARRER OPS 2.0`, use these parent-root commands.
