After meaningful changes, run the smallest relevant validation set. For career-ops core/script changes, prefer `node test-all.mjs --quick` and `node verify-pipeline.mjs`; for dashboard Go changes, run `go test ./...` and `go build -o career-dashboard.exe .` from `career-ops/dashboard`. Keep changes backward-compatible with existing file artifacts unless a contract migration is explicitly intended and documented. Avoid touching user-layer files unless the task is specifically about personalization or data.